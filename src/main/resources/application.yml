spring:
  application:
    name: plantopolis-backend
  datasource:
    url: "jdbc:oracle:thin:@//localhost:1521/XEPDB1"
    username: "plantopolis_user"
    password: "Plantopolis123"
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:tu_email@gmail.com}
    password: ${MAIL_PASSWORD:tu_app_password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true     

server:
  port: 8080

jwt:
  secret: "plantopolisJwtSecretKey2024MuyLargaMinimo256BitsParaHMACSHA"
  expiration: 86400000

logging:
  level:
    com.plantopolis: DEBUG