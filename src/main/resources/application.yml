spring:
  application:
    name: plantopolis-backend

  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/XE
    username: plantopolis_user
    password: Plantopolis123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true

server:
  port: 8080

jwt:
  secret: plantopolis-jwt-secret-key-2024-muy-larga-minimo-256-bits
  expiration: 86400000

logging:
  level:
    com.plantopolis: DEBUG
    